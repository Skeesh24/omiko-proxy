<VirtualHost *:80>
    ProxyRequests On
    ProxyPreserveHost On
 
    ProxyPass / http://${ENV:HOST}:10000/
    ProxyPassReverse / http://${ENV:HOST}:10000/

    ProxyPass /user http://${ENV:HOST}:10000/user
    ProxyPassReverse /user http://${ENV:HOST}:10000/user

    ProxyPass /docs http://${ENV:HOST}:10000/docs
    ProxyPassReverse /docs http://${ENV:HOST}:10000/docs

    ServerName localhost
</VirtualHost>
